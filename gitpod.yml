tasks:
  - command: |
      cd mattermost && docker compose up -d && ..
      cd serge && docker compose up -d & ..
      cd ai-bot && docker compose up -d
ports:
  - port: 8080
    onOpen: open-preview
    visibility: public
  - port: 8065
    visibility: public
    onOpen: open-preview
